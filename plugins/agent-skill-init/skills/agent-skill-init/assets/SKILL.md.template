---
name: my-skill
description: <What it does in one clause.> Use when <explicit trigger phrases / file types / contexts the user might say or do>. <Optionally: what it produces.>
# Optional fields (delete if unused):
# license: MIT
# compatibility: <client/runtime requirement, 1-500 chars — rarely needed>
# metadata:
#   version: "1.0.0"
#   author: "Your Name"
# allowed-tools: Read Write Bash   # space-separated, experimental
---

# My Skill

## Overview
One or two sentences on what this skill does and why it exists.

## When to use
- Trigger 1 (a phrase or context the user might present)
- Trigger 2

## Workflow
1. First step.
2. Second step.
3. Validate / confirm the result.

## Notes
Gotchas, prerequisites, or links to references/ for depth.
<!--
Reminders:
- `name` must equal this directory's name (kebab-case, no consecutive hyphens).
- Keep this body under ~500 lines; push depth into references/ (one level deep).
- Validate with:  skills-ref validate ./my-skill
-->
