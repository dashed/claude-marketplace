# Changelog - postgres-performance

All notable changes to the postgres-performance skill in this marketplace will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [1.0.0] - 2026-06-09

### Added
- Initial addition to the marketplace — a skill for **PostgreSQL query & performance tuning**, scoped to reading `EXPLAIN`/`EXPLAIN ANALYZE` plans, choosing & designing indexes, fixing planner row-estimate errors with statistics, and diagnosing MVCC bloat / `VACUUM`. Authored against the PostgreSQL source tree **19beta1** (stable = PG 18, PG 19 = beta); every EXPLAIN snippet is grounded in the authoritative documentation examples in `perform.sgml`/`explain.sgml` (no live server was used to fabricate output).
- `SKILL.md` (362 lines): mental model (cost-based planner + four levers: EXPLAIN / indexes / statistics / VACUUM-config); cross-skill disambiguation vs `psql`, `postgres-sql`, `postgres-admin`, `postgres-extensions`; the slow-query loop; the EXPLAIN options matrix; an annotated `EXPLAIN ANALYZE` line; reading scans/joins/aggregates and the three signals behind most slow queries; index type & strategy tables; statistics + key planner GUCs; VACUUM/bloat/MVCC with the PG19 `REPACK` change; parallel query & JIT; a perf-extensions pointer table; quick reference; troubleshooting.
- `references/explain.md`: every `EXPLAIN` option in depth, the cost model, anatomy of an `EXPLAIN ANALYZE` line, all extra-statistics lines, a full line-by-line annotated plan walkthrough, output formats, parallel-plan reading, `GENERIC_PLAN`/custom-vs-generic plans, caveats, and `auto_explain`/`pg_overexplain`.
- `references/reading-plans.md`: node-by-node reference (every scan/join/aggregate/sort/set/parallel node), the bottom-up reading method, and a catalogue of diagnosable plan-shape problems → fixes.
- `references/indexes.md`: all six index types with a capability matrix, operator classes (`gin_trgm_ops`, `jsonb_path_ops`, `text_pattern_ops`, …), multicolumn/partial/expression strategies, covering `INCLUDE` + index-only scans + the visibility map, `CONCURRENTLY`/`REINDEX` mechanics, storage parameters, and missing/unused-index queries.
- `references/statistics-and-planner.md`: `pg_stats` columns, statistics-target tuning, `CREATE STATISTICS` kinds, every cost & memory GUC, the `enable_*` diagnostic flags, join-order GUCs, the parallel-query knobs (with the 9.6/10/11 feature split), and JIT.
- `references/vacuum-and-bloat.md`: MVCC tuple versions (`xmin`/`xmax`/dead tuples), what `VACUUM` does (FSM + visibility map), `VACUUM` vs `VACUUM FULL` vs the new PG19 `REPACK`, all `VACUUM` options with version floors, freezing & transaction-ID wraparound, bloat/vacuum-lag diagnosis queries (`pg_stat_user_tables`, `pg_statio_*`), running-vacuum monitoring (`pg_stat_progress_vacuum`), and the autovacuum boundary with `postgres-admin`.
- `references/version-features.md`: ~60 source-cited `feature → minimum PostgreSQL version` rows (EXPLAIN options matrix, plan-display changes, indexes, statistics, parallel query, VACUUM/REPACK, profiling extensions), modeled on the duckdb skill's version-features reference, with a "how to read" preamble and postgresql.org sources.
- Inline `(pgNN+)` version annotations verified against the PostgreSQL docs/release notes; **bedrock** (anything from the old 9.x line) left unannotated. Headline facts captured: the **EXPLAIN options matrix** — `SETTINGS` (pg12), `WAL` (pg13), `GENERIC_PLAN` (pg16), `SERIALIZE`+`MEMORY` (pg17), `BUFFERS` auto-on-with-`ANALYZE` (pg18), and the new `IO`/AIO option (pg19, beta); PG18 plan-display changes (`Index Searches`, two-decimal actual rows, `Disabled:`, B-tree **skip scan**); and the PG19 **`REPACK`** command that deprecates `VACUUM FULL` and adds online `REPACK … CONCURRENTLY`.
