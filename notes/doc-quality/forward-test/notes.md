# Offline doc-quality forward test

Fresh fictional scenario; no frozen fixture, expected label, or live API was used.

The rewrite makes the worker the explicit subject, separates the retry rules, and retains MUST strength, the cancellation exception, 2 retries, 750 ms spacing, the 8 MiB bound, failed-upload retention, code, links, rationale, and unknown duplicate-processing behavior. Rollout evidence is unchanged. The manual ledger and executable interface assertions are recorded in verification.json.

All interface assertions passed. Static comparison reports needs_review and meaning_preservation=not_established despite unchanged preservation inventories. It marks semantic evaluation skipped and link validation not_run. Source-level checks confirmed the local link target and heading; no renderer or remote link checker ran.

Dry-run scope checks confirmed three comparison requests, original-only baseline text, identical baseline/candidate questions and context, both candidates in paired preservation, no filenames in semantic state, selected-section-only analyze, and one request for paired-only. All requests stayed offline. Re-running comparison left both files byte-identical.

The rewrite increased counted words from 138 to 140 while reducing mean words per sentence from 17.25 to 9.33. This is an honest density observation, not a score-based acceptance claim. No claim about Jev's incremental usefulness follows from this offline test.

Documentation alignment clarified whole-file compare scope, temporary excerpt use, dry-run without helper or credentials, exact report/status semantics, request bounds, and the absence of computed quality deltas. No implementation bug was found in these exercised paths.
